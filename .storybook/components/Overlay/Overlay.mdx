import { Meta, Story, Primary, ArgTypes } from '@storybook/blocks'
import * as OverlayStories from './Overlay.stories'

<Meta of={OverlayStories} />

# Overlay

The `Overlay` component is used to render custom images at specific locations on the map, based on the `google.maps.GroundOverlay` class.

<Primary />
<ArgTypes />
