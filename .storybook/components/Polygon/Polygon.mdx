import { Meta, Story, Primary, ArgTypes } from '@storybook/blocks'
import * as PolygonStories from './Polygon.stories'

<Meta of={PolygonStories} />

# Polygon

The `<Polygon />` component allows you to draw polygons on the map.

<Primary />
<ArgTypes />
