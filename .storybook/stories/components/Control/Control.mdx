import { Meta, Story, Primary, ArgTypes } from '@storybook/blocks'
import * as ControlStories from './Control.stories'

<Meta of={ControlStories} />

# Control

Control 组件用于将您的自定义控件渲染到屏幕上。

<Primary />
<ArgTypes />
