import { Meta, Story, Canvas, Controls, Primary, ArgTypes, Source } from '@storybook/blocks'
import * as PolygonStories from './Polygon.stories'

<Meta of={PolygonStories} />

# Polygon

您可通过 `<Polygon />` 在地图上绘制多边形。

<Primary />
<ArgTypes />
